<div class='side-header'>
    <%= link_to "#{current_user.name}", edit_user_path(current_user), class: "user-name" %>
  <%# <a href="#" class='create-room'>チャットを作成</a> %>
  <%= link_to 'チャットを作成', new_room_path , class: "create-room" %>
</div>
<div class='side-rooms'>
  <% current_user.rooms.each do |room| %>
    <div class='side-room'>
    <%= link_to room.name, room_messages_path(room.id), class: 'side-room-name' %>
    </div>
  <% end %>
</div>